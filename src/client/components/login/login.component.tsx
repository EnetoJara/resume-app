import React, { ReactElement } from 'react';

export interface LoginProps {};
export function Login (props: LoginProps): ReactElement<LoginProps> {
    const {  } = props;
    return (<div>

    </div>);
}
